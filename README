To setup vm run:

1. Execute as root:

bash -c "$(curl -fsSL https://raw.githubusercontent.com/JanHanke/config_files/virtual/scripts/create-user.sh)"

2. Execute as user:

bash -c "$(curl -fsSL https://raw.githubusercontent.com/JanHanke/config_files/virtual/scripts/setup.sh)"
